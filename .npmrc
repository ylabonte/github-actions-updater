# pnpm uses sane defaults for the keys we previously listed here
# (auto-install-peers=true, strict-peer-dependencies=false, shamefully-hoist=false
# are all the v8+ defaults). Listing them caused `npm` to warn about unknown
# config keys without changing behaviour, so they were removed.
